source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'rack'
gem 'lkr-codebreaker'

group :development do
  gem 'rubocop'
end

group :test do
  gem 'rspec'
  gem 'rack-test'
end
